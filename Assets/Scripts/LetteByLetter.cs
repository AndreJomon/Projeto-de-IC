using System.Collections;
using System.Collections.Generic;
using UnityEngine.UI;
using UnityEngine;

public class LetteByLetter : MonoBehaviour {

   /* public float letterPause;

    string message;
    Text actionText;

    private void Awake()
    {
        actionText = GameObject.FindGameObjectWithTag("LearnText").GetComponent<UnityEngine.UI.Text>();
    }

    private void StartLetterByLetter()
    {
        message = actionText.text;
        actionText.text = "";
        StartCoroutine(TypeText());
    }

    IEnumerator TypeText()
    {
        foreach (char letter in message.ToCharArray())
        {
            actionText.text += letter;
            yield return new WaitForSeconds(letterPause);
        }
    }*/
}
